[package]
name = "farm-blazer"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "0.15", features = ["bevy_remote", "dynamic_linking", "jpeg", "png"] }
bevy_skein = "0.1"
bevy_rapier3d = "0.28" 
rand = "0.8"
chrono = "0.4"
getrandom = { version = "0.2", features = ["js"] }
getrandom_03 = { package = "getrandom", version = "0.3", features = ["wasm_js"] }
# Set max log levels. This helps avoid unwanted low-severity log spam, which can affect performance.
log = { version = "0.4", features = [
    "max_level_debug",
    "release_max_level_warn",
] }
tracing = { version = "0.1", features = [
    "max_level_debug",
    "release_max_level_warn",
] }

# Idiomatic Bevy code often triggers these lints, and the CI workflow treats them as errors.
# In some cases they may still signal poor code quality however, so consider commenting out these lines.
[lints.clippy]
# Bevy supplies arguments to systems via dependency injection, so it's natural for systems to
# request more than 7 arguments -- which triggers this lint.
too_many_arguments = "allow"
# Queries that access many components may trigger this lint.
type_complexity = "allow"

# Enable a small amount of optimization in the dev profile.
[profile.dev]
opt-level = 1

# Enable a large amount of optimization in the dev profile for dependencies.
[profile.dev.package."*"]
opt-level = 3

# Remove expensive debug assertions due to <https://github.com/bevyengine/bevy/issues/14291>
[profile.dev.package.wgpu-types]
debug-assertions = false

[profile.release]
opt-level = "z"     # Optimize for size (instead of speed)
lto = true          # Enable Link Time Optimization (removes unused code)
codegen-units = 1   # Compile more slowly to find better optimizations
panic = "abort"     # Remove stack trace strings (saves space)
strip = true        # Remove debug symbols automatically
